export const appName = "Lingchu Bot";
export const docsRoute = "/docs";
export const docsImageRoute = "/og/docs";
export const docsContentRoute = "/llms.mdx/docs";

/**
 * Public deployment URL used as the default `metadataBase` and as the
 * basis for canonical / Open Graph / Twitter / RSS absolute URLs.
 *
 * Override at build time via `NEXT_PUBLIC_SITE_URL` for previews or
 * alt domains. The `output: 'export'` build resolves this at build time.
 */
export const SITE_URL = "https://lingchu.zone.id/";

export const gitConfig = {
  user: "xinvxueyuan",
  repo: "lingchu-bot",
  branch: "main",
};
