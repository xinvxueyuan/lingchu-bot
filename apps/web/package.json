{
  "name": "web",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "echo 'web app is temporarily disabled'",
    "build": "echo 'web app is temporarily disabled'",
    "lint": "echo 'web app is temporarily disabled'",
    "check-types": "echo 'web app is temporarily disabled'"
  }
}
