version: 2
updates:
  - package-ecosystem: "pip"
    target-branch: develop
    directory: "/"
    schedule:
      interval: "daily" # Check frequency: daily, weekly, or monthly
    open-pull-requests-limit: 5 # Max simultaneous PRs
    # Optional configurations below — enable as needed
    # ignore:
    #   - dependency-name: "example-package"
    #     versions:
    #       - "1.0.0"
    #   - dependency-name: "another-package"
    #     versions:
    #       - "2.0.0"
    # commit-message:
    #   prefix: "deps"
    #   include: "scope" # Options: scope, dependency-name, version
    # versioning-strategy: increase-if-necessary # Options: auto, increase-if-necessary, widen, increase-if-necessary-and-widen
    # reviewers:
    #   - "github-username"
    # assignees:
    #   - "github-username"
    # labels:
    #   - "dependencies"
