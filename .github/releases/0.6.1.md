# Lingchu Bot 0.6.1

补丁版本，包含 PR #37 的运行时配置持久化、CLI 入口推导和文档修复。

## Highlights

- 运行时配置和机器人状态采用稳定快照、写后校验与有界重试，避免异步 flush 丢失更新。
- 启动时可变设置失败回退到干净默认值，关机时两个配置域独立刷新。
- SQLite PRAGMA 游标生命周期异常 fail-soft 处理。
- `lc-cli` 在没有 `bot.py` 的项目中增强入口推导，并同步 CLI 文档。
