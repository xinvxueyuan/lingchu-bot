# lingc-cli 0.1.0

首个正式发布：Lingchu Bot 的运行时启动器（命令 lc）。

- 安全启动 lc run / 环境侦测 lc env / 诊断 lc doctor / 修复 lc repair
- 依赖生命周期 lc install / uninstall / update
- 配置脚手架 lc init / 数据库迁移 lc db（复用 nonebot_plugin_orm）
- 自身更新 lc self-update；独立 uv workspace 成员。
