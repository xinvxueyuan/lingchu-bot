"""Default configuration for remote_mute handle."""

REMOTE_MUTE_DEFAULTS = {
    "enabled": True,
    "defaults": {
        "mute_duration": 60,
        "default_reason": "管理员操作",
    },
    "policies": {},
}
